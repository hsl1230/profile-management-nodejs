import { PrimaryUserProfileDto } from './primary-user-profile.dto';

describe('PrimaryUserProfileDto', () => {
  it('should be defined', () => {
    expect(new PrimaryUserProfileDto()).toBeDefined();
  });
});
