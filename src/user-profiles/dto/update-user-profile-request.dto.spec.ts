import { UpdateUserProfileRequestDto } from './update-user-profile-request.dto';

describe('UpdateUserProfileRequestDto', () => {
  it('should be defined', () => {
    expect(new UpdateUserProfileRequestDto()).toBeDefined();
  });
});
